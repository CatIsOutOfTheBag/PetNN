# Что в голове у женщины? Или как сочетание квартиры, внешности и любви к року влияют на женскую любовь
import numpy as np

# функция активации
def act(x):
    return 0 if x<0.5 else 1

# функция пропуска через НС параметров
def go(house, rock, attr): # квартира, любовь к року, красота
    x = np.array([house, rock, attr]) # формирование вектора входного сигнала
    # параметры принимают значения [0,1]
    w11 = [0.3, 0.3, 0] # веса для первого нейрона скрытого слоя
    w12 = [0.4, -0.5, 1] # веса для второго нейрона скрытого слоя
    weight1 = np.array([w11,w12]) # объединение весов в матрицу 2x3
    # 2x3 - два нейрона по три входных связи
    weight2 = np.array([-1,1]) # вектор связей для выходного нейрона
    
    # вычисление суммы для скрытых нейронов - матричное умножение: веса на входной вектор х
    sum_hidden = np.dot(weight1,x) # на выходе сумма на каждом нейроне
    print("Значения сумм на нейронах скрытого слоя: " + str(sum_hidden))
    
    # исползуя полученную сумму мы ее пропускаем через ф-ию активации
    # получаем вектор выходных значений с каждого нейрона скрытого слоя
    out_hidden = np.array([act(x) for x in sum_hidden])
    print("Значения на выходах нейронов скрытого слоя: " + str(out_hidden))
    
    # вычисляем сумму на выходном нейроне последнего слоя
    sum_end = np.dot(weight2, out_hidden)
    y=act(sum_end) # пропускаем сумму через функцию активации
    print("Выходное значение НС: "+str(y)) # получаем выходное значение НС
    
    return y
    
# сейчас проверим, понравишься ли ты женщине, если у тебя есть квартира, ты не любишь
# тяжелый рок, но и внешностью не вышел
house = 1
rock = 0
attr = 0

res = go(house, rock, attr)
if res == 1:
    print("Love")
else:
    print("No Love")
    
# Не расстраивайся, просто она тебя не достойна!
